<nav class="navbar is-fixed-top" role="navigation" aria-label="main navigation" >
  <div class="navbar-brand">
    <%= link_to "ELEU. COMMONAGE", root_path, class: "navbar-item" %>
    <% if user_signed_in? %>
      <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
        <span aria-hidden="true"></span>
        <span aria-hidden="true"></span>
        <span aria-hidden="true"></span>
      </a>
    </div>

    <div id="navbar" class="navbar-menu">
      <div class="navbar-start">
        <%= link_to "My Profile", profile_show_path(@current_user), class: "navbar-item" %>

        <a class="navbar-item">
          Messages
        </a>

        <div class="navbar-item has-dropdown is-hoverable">
          <a class="navbar-link">
            More
          </a>

          <div class="navbar-dropdown">
            <a class="navbar-item">
              Resources
            </a>
            <a class="navbar-item">
              About
            </a>
            <a class="navbar-item">
              Contacts
            </a>
            <hr class="navbar-divider">
            <a class="navbar-item">
              Report an issue
            </a>
          </div>
        </div>
      </div>
    <% end %>
    <div class="navbar-end">
      <div class="navbar-item">
        <% if current_user.admin? || current_user.executive? %>
          <%= link_to "New post", new_post_path, class: "button is-primary" %>
        <% end %>
      </div>
      <div class="navbar-item">
        <div class="buttons">
          <% if user_signed_in? %>
            <%= link_to 'Sign out', destroy_user_session_path, class: "button is-light" %>
          <% else %>
            <%= link_to "Sign Up", new_user_registration_path, class: "button is-primary" %>     
            <%= link_to "Sign in", new_user_session_path, class: "button is-light" %>
          <% end %> 
        </div>
      </div>
    </div>
</nav>
